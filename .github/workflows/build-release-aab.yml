name: Build Release AAB (Play Store)

on:
  workflow_dispatch:
    inputs:
      version_name:
        description: 'Version name (es. 1.0.0)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Install Android platform & build-tools
        run: |
          SDKMANAGER="$(find "$ANDROID_HOME/cmdline-tools" -name sdkmanager 2>/dev/null | head -1)"
          yes | "$SDKMANAGER" --licenses > /dev/null 2>&1 || true
          "$SDKMANAGER" "platforms;android-35" "build-tools;35.0.0" "platform-tools"

      - name: Create local.properties
        working-directory: MeteoRadar
        run: echo "sdk.dir=$ANDROID_HOME" > local.properties

      - name: Make gradlew executable
        working-directory: MeteoRadar
        run: chmod +x gradlew

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Decode Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > /tmp/meteoradar-release.keystore

      - name: Build Release AAB
        working-directory: MeteoRadar
        env:
          KEYSTORE_PATH: /tmp/meteoradar-release.keystore
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: meteoradar
        run: |
          BUILD_NUM=${{ github.run_number }}
          ./gradlew bundleRelease assembleRelease -PbuildNumber=$BUILD_NUM --stacktrace

      - name: Rename AAB
        run: |
          mkdir -p /tmp/aab-out
          cp MeteoRadar/app/build/outputs/bundle/release/app-release.aab \
             /tmp/aab-out/MeteoRadar-release.aab

      - name: Rename APK
        run: |
          cp MeteoRadar/app/build/outputs/apk/release/app-release.apk \
             /tmp/aab-out/MeteoRadar-v1.${{ github.run_number }}.apk

      - name: Upload AAB as artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeteoRadar-release-aab
          path: /tmp/aab-out/MeteoRadar-release.aab
          retention-days: 90

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeteoRadar-release-apk
          path: /tmp/aab-out/MeteoRadar-v1.${{ github.run_number }}.apk
          retention-days: 90

      - name: Create GitHub Release with AAB + APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: release-${{ github.run_number }}
          name: "MeteoRadar v1.${{ github.run_number }} — Release"
          body: |
            ## MeteoRadar v1.${{ github.run_number }}

            ### Test rapido
            Scarica `MeteoRadar-v1.${{ github.run_number }}.apk` e installalo direttamente sul dispositivo.

            ### Play Store
            Carica `MeteoRadar-release.aab` su [Google Play Console](https://play.google.com/console) → Produzione → Crea nuova versione.
          files: |
            /tmp/aab-out/MeteoRadar-release.aab
            /tmp/aab-out/MeteoRadar-v1.${{ github.run_number }}.apk
          prerelease: false
